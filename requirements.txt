# Core dependencies
numpy>=1.26.0,<2.0.0
sentence-transformers>=4.1.0
scikit-learn>=1.6.1
torch>=2.6.0
transformers>=4.46.3
nltk>=3.9.1
rank-bm25>=0.2.2

# HTTP client (constrained for supabase compatibility)
httpx>=0.24,<0.28

# LLM API clients
openai>=1.61.1
litellm>=1.59.1
ollama>=0.3.3

# Evaluation metrics
rouge-score>=0.1.2
bert-score>=0.3.13

# Utilities
tqdm>=4.66.1
pandas>=2.2.3
python-dotenv>=1.0.1
hf_xet

# Note: pathlib is included in Python 3.4+ standard library
# Note: CUDA support is integrated into PyTorch, no separate package needed

# Additional requirements for the Flask API
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0  # For production deployment


# Supabase Integration Requirements for AgenticMemory

# Core Supabase client
supabase>=2.3.0

# PostgreSQL adapter (required by Supabase)
psycopg2-binary>=2.9.0

# Vector operations (if using direct PostgreSQL access)
pgvector>=0.2.0

# Additional dependencies for enhanced functionality
python-dotenv>=1.0.0  # For loading .env files
asyncpg>=0.29.0       # Async PostgreSQL driver (optional)
sqlalchemy>=2.0.0     # SQL toolkit (optional, for advanced queries)

# Data manipulation and analysis
pandas>=2.0.0         # For data analysis (optional)
numpy>=1.24.0         # Already required by sentence-transformers
